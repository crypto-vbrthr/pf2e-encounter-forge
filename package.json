{
  "name": "pf2e-encounter-forge",
  "version": "0.1.0-alpha.13.4",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test --test-concurrency=1 tests/*.test.js",
    "check": "npm test && find scripts -name '*.js' -print0 | xargs -0 -n1 node --check"
  },
  "engines": {
    "node": ">=20"
  }
}
